import { HighlightPrice } from './highlight-price';

describe('HighlightPrice', () => {
  it('should create an instance', () => {
    const directive = new HighlightPrice();
    expect(directive).toBeTruthy();
  });
});
