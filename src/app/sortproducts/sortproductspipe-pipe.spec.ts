import { SortproductspipePipe } from './sortproductspipe-pipe';

describe('SortproductspipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortproductspipePipe();
    expect(pipe).toBeTruthy();
  });
});
